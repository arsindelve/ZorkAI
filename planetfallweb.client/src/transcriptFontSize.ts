/**
 * Planetfall's base transcript font size, in pixels.
 *
 * This is the "Medium" rung of the transcript font size preference - every other rung
 * is a multiple of it (see TRANSCRIPT_FONT_SCALE in the shared package). Zork keeps its
 * own base, so each game's default text size stays exactly as it shipped.
 */
export const TRANSCRIPT_BASE_FONT_SIZE_PX = 16;
